package personalloan

import personalloan.LoanApplication
import personalloan.IncomeSource

rule "no NINJAs"
	salience 10
	dialect "mvel"
	when
		app : LoanApplication( )
		not (IncomeSource( )) 
	then
		app.setApproved( false );
		app.setExplanation( "no NINJAs" );
		retract( app );
end
